seed: 0

gpu: 'cuda:1'

model:
  custom: true
  arch: 'ResNet34'
  tvision:
    name: 'resnet34'
    args: {}

data:
  flag: '100'
  dir: './cifar-data'
  batch_size: 128


train:
  epochs: 300
  lr: 0.1
  lr_scheduler: 'multistep'
  step: 100
  milestones: [150, 250]
  gamma: 0.1
  lr_min: 0.
  lr_max: 0.2
  weight_decay: 0.00005
  momentum: 0.9

opt:
  level: 'O2'
  loss_scale: 1.0
  store: true

logger:
  wandb: true
  project: 'CIFAR100N'
  run: 'RESNET34'

save:
  epochs: 50